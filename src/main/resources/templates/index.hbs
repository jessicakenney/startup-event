{{#partial "content"}}

    <h2>Event List</h2>

    {{#if events}}
        {{#each events}}
        <div class="col-md-4">
        <div class = "well">
            <p>{{name}}
            <p>{{description}}</p>
            <p>{{date}}</p>
            <a href="/events/{{id}}">Event details...</a></p>
        </div>
        </div>
        {{/each}}
    {{else}}
        <p>No Events Posted</p>
    {{/if}}

    <footer class="footer">
        <div class="container">
        </div>
    </footer>
{{/partial}}

{{> layout.hbs}}