{{#partial "content"}}

    {{#if addAttendee}}
        <h3>Add Attendee</h3>
        <form action="/events/{{addAttendee.id}}/attendees" method="post">
            <div class="form-group">
                <label for="name">Name</label>
                <input id="name" class="form-group" name="name" type="text" required>
            </div>

    {{else}}

        {{#if editEvent}}
            <h3>Update the Event</h3>
            <form action="/events/{{editEvent.id}}/update" method="post">
                <div class="form-group">
                    <label for="name">Event Name</label>
                    <input id="name" name="name" type="text" value="{{editEvent.name}}">
                    <br>
                    <label for="description">Event Description</label>
                    <input id="description" name="description" type="text" value="{{editEvent.description}}">
                    <br>
                    <label for="date">Enter Date</label>
                    <input id="date" name="date" type="date" value="{{editEvent.date}}" required>
                </div>
                <br>
        {{else}}

        <h3>Add a New Event</h3>
        <form action="/events/new" method="post">
            <div class="form-group">
                <label for="name">Enter Name</label>
                <input id="name" class="form-group" name="name" type="text" required>
                <br>
                <label for="description">Enter Description</label>
                <input id="description" class="form-group" name="description" type="text" required>
                <br>
                <label for="date">Enter Date</label>
                <input id="date" class="form-group" name="date" type="date" value="2017-10-22"required>
            </div>
            <br>
        {{/if}}
    {{/if}}

    <button type="submit" class="btn btn-default">Add</button>
    </form>

{{/partial}}

{{> layout.hbs}}