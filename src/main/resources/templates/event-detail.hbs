{{#partial "content"}}

    <h4>Name:</h4>
    <p>{{event.name}}</p>
    <h4>Description:</h4>
    <p> {{event.description}}</p>
    <a href="/events/{{event.id}}/update">Edit</a>

    <h4>Attendees:</h4>
    <p> {{ event.attendees}}</p>
    <a href="/events/{{event.id}}/Add Attendee">Edit</a>

    <!--{{#if event.published }}-->
        <!--<span class="glyphicon glyphicon-star" aria-hidden="true"></span>  Published-->
    <!--{{else}}-->
        <!--<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Draft-->
    <!--{{/if}}-->

    </p>
{{/partial}}

{{> layout.hbs}}
