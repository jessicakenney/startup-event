{{#partial "content"}}
  <div class="well">
    <h4>Name:</h4>
    <p>{{event.name}}</p>
    <h4>Description:</h4>
    <p> {{event.description}}</p>
    <a href="/events/{{event.id}}/update">Edit</a>
  </div>

  <div class="well">
    <h4>Attendees:</h4>
        {{#each event.attendees}}
            <li>{{ this }} </li>
            <!--<a href="/events/{{id}}">Event details...</a></p>-->
        {{/each}}

    <!--<p> {{ event.attendees}}</p>-->
    <a href="/events/{{event.id}}/addAttendees">Edit</a>
  </div>

    <!--{{#if event.published }}-->
        <!--<span class="glyphicon glyphicon-star" aria-hidden="true"></span>  Published-->
    <!--{{else}}-->
        <!--<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Draft-->
    <!--{{/if}}-->

    </p>
{{/partial}}

{{> layout.hbs}}
